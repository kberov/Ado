<!DOCTYPE html>
<html>
  <head><%= include 'layouts/header'; %></head>
  <body><%= content %></body>
  <!-- Produced by
% if(stash->{controller}){
%=  'Controller: ' .stash->{controller}.'; Action: '.stash->{action};
% }else{ 
%=  'Template:'.stash->{'mojo.captures'}{template};
% }
  -->
</html>
