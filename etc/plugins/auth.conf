#!/usr/bin/perl
use strict;
use warnings;
use utf8;

#$MOJO_HOME/etc/plugins/auth.conf
#Default configuration for Ado::Plugin::Auth
{
    #Add your routes definitions here.
    routes => [

        #links to forms, suitable for embedding in menus
        {   route => '/authbar',
            via   => ['GET'],
            to    => {
                cb => sub { shift->render('authbar') }
            },
        },

        #the login form
        {   route => '/login/:method',
            via   => ['GET'],
            to    => {
                auth_method => 'local',
                cb          => sub { shift->render('login') }
            },
        },

    ],
};
